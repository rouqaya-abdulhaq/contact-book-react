import React from "react";
import "./card.css";
import Form from "../form/form";
const Card = () =>{
    return(
        <div className="cardWrapper">
        <h1>card component</h1>
        <Form/>
        </div>

    );
}

export default Card;