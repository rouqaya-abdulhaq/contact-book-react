import React from "react";
import "./card.css";
import Form from '../../containers/form/form';

function Card(props){
        return(
            <div className="cardWrapper">
            <Form/>
            </div>
        ); 
}

export default Card;